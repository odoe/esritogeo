require './esritogeo'
require './spec_helper'

class SampleGeo
  constructor: () ->

  runSample: () ->
    util = new GeoUtil()
    helper = new SpecHelper()
    util.convertEsriToGeo helper.dirty(), (errors, result)->
      console.log result
